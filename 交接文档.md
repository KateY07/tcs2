# TCS 项目交接

更新日期：2026-09-26

## 项目目的

TCS 是独立于系统 OpenSSH Server/`sshd` 的远程运维通道。主控端用 OpenSSH 格式私钥认证被控端，双方通过裸 TCP 上运行的 TCS/1 握手建立加密会话；加密会话承载现有 `/v1/health`、`/v1/exec`、`/v1/upload` HTTP 请求。不使用 TLS/X.509，也不走 SSH transport/channel。

项目的正式约束基线是 [固化目标.md](固化目标.md)，协议文本是 [实现tcs 传输协议.md](实现tcs%20传输协议.md)。继续实现前先对照这两份文档；若要改变目标，先明确更新基线。

## 当前状态

- .NET 10，Windows x64 Native AOT。`tcs.exe` 是主控端；同一程序按文件名 `tcsd.exe` 启动时作为被控端。默认 TCP 端口 `10122`，默认双栈监听。
- 密钥位置现已固定：主控 `%USERPROFILE%\.ssh\id_ed25519`；被控 `.ssh\tcs_host_key`，授权 `.ssh\authorized_keys`。本机公钥从私钥推导，同名 `.pub` 不读取、不检查、不修改。信任记录 `.ssh\tcs_known_hosts`，兼容旧 `.ssh\tcs_host_key.pub`（用于对端身份），生效时明确提示。生产 CLI、公开类、配对和服务均不接收密钥路径。
- 已实现一次纯数据配对文件导入、首次连接核对指纹、此后固定校验，无需回执。主控执行 `tcs pairing export --output .\主控端.tcs-pair`，被控执行 `tcsd pairing import .\主控端.tcs-pair`。私钥必须由用户安装前准备；安装先选角色并验证私钥及签名能力，私钥缺失/损坏/带口令时停止并提示，缺少 `.pub` 不阻止。导入不调用 ssh-keygen；私钥不传输，身份变化不自动覆盖。
- 本次发布版本为 `2026.09.26.4`，安装包 `_package\tcs-manual-2026.09.26.4.exe`，本地 ZIP 为 `D:\pub\tcs\2026.09.26.4.zip` 及同名 `.sha256`。含程序、README 和安装脚本，不含任何密钥、公钥或授权列表。安装到 `%LOCALAPPDATA%\TCS`，把此路径追加至当前用户 Path，并检查重复；不提权、不注册服务、不修改防火墙。GitHub 发布状态以仓库 Release 为准；先前交付的本地 `.3` 包保留不覆盖。
- 本次发布目标 `2026.09.26.6`，发布状态以 GitHub Release 为准；上一项 `.4` 是历史版本。当前手动部署只需启动 `tcsd` 并保持窗口运行，数据固定 `%LOCALAPPDATA%\TCS\data`，`--data` 已禁止。执行远程指令要求被控端 Python，`--python` 仍可指定解释器。生产 API 为 `new tcs(host, port)` 和 `new tcsd()`；路径重载仅存在于独立测试工程的条件编译。
- 用户部署 `.6` 后已完成 ALIPC 实测：原信任有效、健康检查成功，Python 3.14.4；8 次指令/4 路并发无串扰，45,056 字节上传哈希一致并清理测试文件，已安装二进制哈希与发布构建一致。详见 `固定路径审查.md`。
- Windows Service 的实验代码在 `DaemonService.cs` 和 `_installer\install-service.ps1`，尚未通过管理员权限下的真实 SCM 安装验收。当前建议按手动部署包工作。

## 关键实现位置

| 文件 | 内容 |
|---|---|
| `Program.cs` | CLI 分流：客户端、守护端；改名默认客户端，无遗留目录入口 |
| `tcs.cs` | 主控 TCP 客户端、握手、健康检查、命令和上传 |
| `tcsd.cs` | TCP 监听、认证、解密请求、运行 Python、保存上传 |
| `TcsWire.cs` | TCS/1 握手消息、加密帧、序号和会话状态 |
| `TcsCrypto.cs` | OpenSSH 密钥读写、签名验证、X25519/HKDF/AEAD |
| `HostKeys.cs` | 隔离测试用密钥生成；发布 CLI 已禁止生成入口 |
| `Deployment.cs` | 固定路径、安装前私钥验证、用户生成脚本及失败提示 |
| `tests/TransportHarness.csproj` | 链接生产类的隔离测试入口，不打入发布程序，测试允许临时路径 |
| `deployment_regression_test.py` | 发布 CLI 禁止路径覆盖、密钥预检查及安装顺序测试 |
| `Pairing.cs` | 纯数据配对导出、严格解析、指纹确认、幂等授权导入 |
| `HostTrust.cs` | 首次探测确认、按连接目标固定主机公钥、旧固定公钥兼容 |
| `pairing_regression_test.py` | 配对、篡改、非交互拒绝、伪造签名、主机换钥回归测试 |
| `README.md` | 手动安装及使用说明 |
| `_installer\install-user.bat` | 免提权安装入口 |
| `_installer\set-user-path.ps1` | 当前用户 Path 更新及去重 |
| `alipc使用说明.md` | 已部署被控端 alipc 的本机连接说明 |

## TCS/1 大致安全流程

2026-09-26 配对改动已通过 Native AOT 发布、配对回归及 `crash_regression_test.py`：包含双栈健康检查、Python 执行、上传内容校验；另外实测终端导入和首次连接输入 `yes`，随后非交互连接直接返回健康状态。之后用户在 alipc 安装并启动新版，主控以用户提供的完整指纹成功固定主机公钥、健康检查返回 ok；随后不带指纹执行 Python，返回机器名 alipc、Python 3.14.4、退出码 0。此次 alipc 验证未测试上传。

1. 主控发送客户端公钥 blob、临时 X25519 公钥和随机数。
2. 被控端要求客户端公钥存在于 `authorized_keys`，再返回其主机公钥、临时公钥、随机数及主机私钥签名。
3. 主控端精确比对本地固定的服务端公钥，并验证签名；主控私钥对握手上下文签名，被控端用授权公钥验证。
4. 双方以临时 X25519 密钥和握手 transcript 派生会话密钥，使用 ChaCha20-Poly1305 认证加密数据帧。密码库由 BouncyCastle 提供。
5. HTTP 请求被拆成加密 DATA 帧；被控端处理后返回加密响应。

请以 `TcsWire.cs`、`TcsCrypto.cs` 实际代码为准。规范和实现仍有下述差距，不能只凭握手成功宣称完整协议验收。

## 已有环境和运维通道验证

主控端本机已用默认私钥连接被控端 `alipc` 的 `10122` 端口。已保存的被控端公钥文件为 `_package\alipc-3388.pub`；文件名中的 `3388` 是历史名称，已通过当前 `10122` 连接验证主机密钥匹配。被控端用户是 `me`，远程 Python 为 `3.14.4`，解释器位于该用户本地 Python 安装目录。

最近一次验证中：

- `health` 返回 `{"status":"ok"}`。
- 只读 Python 查询成功，退出码 0、stderr 为空、未超时，返回主机名 `alipc`、Windows 11 build `10.0.26200`、Python `3.14.4`。
- 查询读取了远端系统信息，没有修改远端配置。

用户明确说明这次测试从一开始就在公网环境。上面的成功结果证明测试当时的网络路径、TCS 端口和密钥认证可用；当前记录没有保留 DNS 最终解析 IP、路由路径或抓包，不能据此推断流量一定穿越公网、经过何种 NAT，或确认公网防火墙策略。需要复测公网路径时，先记录 `Resolve-DnsName alipc`、目标 IP、TCP 10122 测试结果，以及客户端与服务端时间戳。不要用更早的直连链路本地 IPv6 测试替代这次公网前提。

## 已知差距和后续优先项

这是接手时最重要的工程事项：

1. **规范要求单连接多路并发，当前没有实现。** 每个 TCP 连接只读取一个 HTTP 请求；OPEN/DATA/END 固定使用 stream ID 1。监听循环可并行处理多个 TCP 连接，因此多个单独命令连接可能并行，但这不等同于规范要求的同一连接多路复用。
2. **规范要求流式上传，当前实现会整体缓冲。** 客户端 `tcs.cs` 使用 `File.ReadAllBytesAsync`；被控端在 `tcsd.cs` 将完整请求读入 `HttpBytes`/`byte[]` 后解析 multipart 并写文件。大文件会占用相应内存，并不符合目标中的无上限内存缓冲禁止要求。
3. **验收覆盖不完整。** 当前记录证明一次健康检查、一次只读 Python 执行和此前本机测试；没有完整证据证明未授权密钥拒绝、公网抓包保密、篡改/重放/截断拒绝、大文件流式完整性、并发隔离、连接中断和资源上限。
4. **密钥支持边界。** 用户 README 使用 `ssh-keygen -t ed25519`。当前私钥读取路径只解析未加密 OpenSSH 私钥；口令保护密钥和 ssh-agent 不支持。默认只尝试 `id_ed25519`，不自动遍历 `.ssh` 下的所有身份。
5. **守护端授权列表在构造时读入内存。** 修改 `authorized_keys` 后需重启 `tcsd` 才会生效。
6. **遗留 mTLS HTTP 代码仍在 `Program.cs`。** 新模式通过程序名/参数进入；默认直接运行 `tcs.exe`、不带新 CLI 标志时仍可能走旧 HTTP 参数解析路径。改启动行为前需先评估向后兼容。

建议先补齐协议差距和自动化安全测试，再重新确认固化目标是否全部通过；不要把当前可用的双机基本连接视为完整生产级验收。

## 构建、测试和打包

仓库目标框架为 `net10.0`，RID `win-x64`，启用 `SelfContained` 和 `PublishAot`。最近 AOT 产物生成在 `_package\service-aot-build\tcs.exe`；打包脚本将该二进制复制两份并分别命名为 `tcs.exe`、`tcsd.exe`。重新打包前应从当前源码重新发布，不能直接把旧 `_package` 产物当作最新代码。

手动安装包生成脚本：`_installer\package-manual.ps1`。版本默认值已更新为 `2026.09.24.2`；本地发布目标是 `D:\pub\tcs\2026.09.24.2.zip` 和同名 `.sha256`。版本不得覆盖；发布前确认目标不存在，并对照包内文件清单确认没有私钥、`authorized_keys`、日志或临时测试文件。

用户 Path 去重函数已测试空 Path、末尾分号、重复路径、大小写、斜杠和 `%LOCALAPPDATA%` 等 8 种情况，并检查重复运行幂等。之前安装器脚本在隔离目录中验证了免提权复制，但新加入 Path 修改的版本仍应做一次真实用户侧安装验收。

项目根目录 `test-logs` 中的部分日志属于较早 mTLS 版本。`regression_20260924_190345.log` 记录旧工作目录的 csproj XML 注释解析错误；`smoke-server.out.log` 是旧 HTTPS 监听结果，不能当作当前 TCP 协议测试证据。

## 仓库状态

交接时最近提交为 `908696c Publish standalone tcs and tcsd AOT binaries`。工作区有未提交变更：`Program.cs`、`tcs.csproj`、`README.md` 修改；新增 `DaemonService.cs`、`HostKeys.cs`、`InstallVerification.cs`、`README-mTLS.md`、`_installer/` 和 `alipc使用说明.md`。`交接文档.md` 也属于当前新增交接内容。提交或清理之前先检查 `git status` 和 diff，保留所有已有用户更改。

## 敏感信息处理

本机主控私钥位于 `%USERPROFILE%\.ssh\id_ed25519`，不要读取到日志、不要复制到被控端、不要写入交接文档或发布包。被控端 host private key 同样只留在被控端。交接时只传递受信任的 `.pub` 文件和指纹，并通过独立可信渠道核对。
