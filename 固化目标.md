# tcs2 固化目标

本文件是 tcs2 后续设计、实现、测试和打包的唯一目标基线。除非明确修改本文件，否则不得擅自改变以下目标或引入偏离目标的替代架构。

## 一、最终目标

构建一个独立运行的 tcs 服务：主控端可以使用现有 OpenSSH 格式的密钥对，通过裸 TCP 连接被控端；连接内容经过加密后承载原有 HTTP 请求，服务端继续执行原有 HTTP API。

目标链路固定为：

```text
主控端 tcsctl
    │
    │ 裸 TCP
    │
    │ tcs 安全传输层
    │  SSH 格式密钥认证
    │  临时会话密钥协商
    │  AEAD 加密
    ▼
被控端 tcs
    │
    │ 解密并还原 HTTP 请求
    ▼
原有 HTTP API
    ├─ GET  /v1/health
    ├─ POST /v1/exec
    └─ POST /v1/upload
```

## 二、不可变约束

1. tcs 必须独立运行，不要求被控机预装或运行 `sshd`。
2. 不采用标准 SSH transport，不依赖 SSH channel、SSH subsystem 或系统端口转发。
3. 不使用 TLS，不要求服务端 X.509 证书或自签名证书。
4. 网络入口使用裸 TCP；安全性全部由 tcs 安全传输层提供。
5. 主控端复用 OpenSSH 格式的私钥和公钥。
6. 被控端使用标准 `authorized_keys` 格式保存主控端公钥。
7. 主控端私钥永远不得复制到被控端。
8. SSH 密钥只用于身份认证和签名，不直接作为对称加密密钥。
9. HTTP API 的路径、方法、JSON 结构、multipart 上传形式和响应语义必须保持不变。
10. `/v1/exec` 必须继续支持超时、独立输出和并发执行。
11. `/v1/upload` 必须继续支持流式上传，不能把完整文件无上限地读入内存。
12. 安全实现必须使用成熟密码库；不得自行实现 AES、ChaCha20、RSA、椭圆曲线或密钥交换算法。

## 三、安全传输协议

安全传输层采用混合加密：

1. 双方交换临时密钥材料和随机数。
2. 主控端使用 SSH 私钥对握手上下文进行签名。
3. 被控端使用 `authorized_keys` 中的公钥验证主控端签名。
4. 双方通过临时密钥协商得到本次连接的会话密钥。
5. 使用 AES-GCM 或 ChaCha20-Poly1305 加密后续 HTTP 字节流。
6. 每个加密帧必须包含版本、序号或 nonce、长度、密文和认证标签。
7. 必须拒绝重复帧、乱序重放、截断帧、长度溢出和认证标签错误。
8. 被控端必须具备自己的稳定身份密钥；主控端必须通过预置指纹或等价方式验证被控端身份，防止主动中间人攻击。允许首次连接时展示经握手签名验证的主机公钥指纹，由用户通过可信渠道核对并明确确认后保存；后续必须固定验证，不得自动接受变化。首次确认前不得发送业务请求，无需人工回传回执文件。
9. 握手失败、认证失败、密文校验失败和协议版本不匹配时，连接必须立即关闭并记录审计信息。

## 四、HTTP 兼容要求

安全传输层解密后，必须将数据交给现有 HTTP 处理逻辑。业务层不得改成 SSH 命令接口、SFTP 接口或自定义 RPC 接口。

必须保持：

- `/v1/health` 的响应结构；
- `/v1/exec` 的请求字段、超时行为、stdout/stderr 和退出码；
- `/v1/upload` 的 multipart/form-data 形式、文件名清理和流式写盘；
- 多个请求同时执行时的隔离性和并发性；
- 审计日志记录和异常可见性。

主控端需要提供 `tcsctl` 或等价客户端，将现有 HTTP 请求编码到安全传输层中。普通 `curl http://...` 不属于目标客户端，因为裸 TCP 入口不是普通 HTTP 端口。

## 五、明确排除项

以下方案不属于 tcs2 目标，不得在未修改本文件的情况下采用：

- 安装或依赖系统 `sshd`；
- 将 tcs 改造成 SSH server；
- 使用 SSH channel 或 SSH subsystem 承载 HTTP；
- 使用 TLS/mTLS 或服务端 X.509 证书；
- 使用预共享对称密钥替代 SSH 密钥对；
- 将 HTTP body 改成不兼容原 API 的自定义业务协议；
- 让主控端私钥出现在被控端；
- 自行实现密码算法或未经验证的加密协议组件。

## 六、验收标准

实现完成后必须至少验证：

1. 使用现有 OpenSSH 私钥和对应公钥完成认证。
2. 未登记在 `authorized_keys` 中的公钥被拒绝。
3. 主控端私钥不出现在被控端文件系统、日志或网络明文中。
4. 被动抓包无法读取 HTTP 请求、脚本内容和上传文件内容。
5. 主动篡改、重放、截断和乱序数据均被拒绝。
6. `/v1/health`、`/v1/exec` 和 `/v1/upload` 的 API 行为与当前版本一致。
7. 多个 `/v1/exec` 请求可以并发执行，互不串输出。
8. 大文件上传采用流式处理并完成完整性校验。
9. 连接断开、超时、认证失败和子进程异常均可观察、可审计，不能静默吞掉异常。
10. 安装包不包含任何本机测试密钥、私钥、授权文件或运行日志。
