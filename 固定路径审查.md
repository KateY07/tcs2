# 固定密钥路径审查与回归记录

日期：2026-09-26。范围：当前工作区生产源码及 Windows x64 Native AOT 构建，不代表 GitHub 旧 Release 已更新。

## 要求

密钥必须位于运行账户自身 `.ssh` 的固定位置，不允许也无需传入路径。主控私钥为 `id_ed25519`，被控私钥为 `tcs_host_key`；本机公钥从私钥推导，同名 `.pub` 不读取、不检查、不修改。对端授权及信任仍严格校验。

## 修复后的静态审查

结论：本次固定路径要求通过，未发现仍可从生产入口指定密钥目录的问题。

- `Deployment.cs` 集中确定用户目录下固定位置，没有 TCS 密钥路径环境变量或配置覆盖。
- `Program.cs` 拒绝旧路径参数；改名也按主控处理，已无 mTLS `-i <目录>` 分支。
- 生产 `tcs` 构造函数只接受主机和端口；`tcsd` 无参构造。身份探测、配对导入导出和服务接口无密钥路径参数。
- 参数化测试接口只在独立测试项目的 `TCS_TESTING` 条件下编译。生产工程拒绝该符号，排除密钥生成及旧安装验证辅助类。
- 旧启动脚本不再在程序目录生成密钥或传递密钥路径。服务脚本使用 LocalSystem 自身固定 `.ssh`，要求预置身份，不生成、迁移或临时授权密钥。
- 历史 mTLS 实现及脚本保留为 `legacy/*.txt`，不编译、不打包；旧证书/安装入口仅提示迁移，不执行旧流程。

## 静态审查通过后执行的回归

- Native AOT 发布及隔离测试项目构建成功，无编译警告或错误。
- 生产程序集反射检查：公开构造、探测和配对接口无密钥路径参数；不含测试生成器。
- 生产工程拒绝 `TCS_TESTING` 的负向构建检查通过。
- 发布 CLI：路径参数和改名后的 `-i` 绕过均被拒绝。
- 私钥验证：缺少 `.pub` 可用；错误/损坏 `.pub` 被忽略且不改写；导出使用私钥推导的公钥；缺失、损坏和带口令私钥被拒绝。
- 配对：拒绝篡改、错误指纹、伪造服务端签名和主机换钥；重复导入幂等；首次确认后直接连接；旧固定公钥明确提示。
- 传输：IPv4/IPv6、Python 执行、上传字节一致；8 组并发执行/上传未串扰；握手断开和部分认证截断按预期处理。
- 用户 Path 去重 8 个案例通过。服务脚本语法及固定路径静态检查通过，未运行 SCM 安装或修改防火墙。
- 修正后的 Native AOT 主控不传任何密钥参数，执行 `tcs --host alipc --operation health`：使用已保存的固定信任记录，返回 `{"status":"ok"}`。未在 ALIPC 替换程序或重装服务。

传输回归使用独立测试项目及临时身份，未修改用户真实密钥。上述结论仅针对本次修复及所列测试，不等于完整协议或所有部署环境的安全认证。

## ALIPC 部署后验收

用户在 ALIPC 部署后再次验收：全程不指定密钥路径，沿用原主机信任。健康检查返回 ok；机器名 alipc、Python 3.14.4；8 次指令、4 路并发输出无串扰；45,056 字节测试上传的 SHA256 一致，测试文件已删除。远端已安装 tcsd.exe 的 SHA256 与本次 Native AOT 构建一致。此为手动部署验收，不包含 SCM 服务安装。
